package org.octopusden.octopus.components.registry.api.enums;

public enum VersionControlSystemType {
    GIT,
    /**
     * Whiskey VCS type.
     */
    EXTERNAL,
    /**
     * More than one used VCS repositories.
     */
    MULTIPLY
}
