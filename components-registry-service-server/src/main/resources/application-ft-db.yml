spring:
  datasource:
    url: "jdbc:h2:mem:crs;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;NON_KEYWORDS=KEY,VALUE"
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

components-registry:
  auto-migrate: true
