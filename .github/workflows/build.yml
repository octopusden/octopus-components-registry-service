name: Gradle Compile & UT

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-build-and-deploy:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-build.yml@v2.2.1
    with:
      flow-type: hybrid
      java-version: '21'
      docker-image: components-registry-service
