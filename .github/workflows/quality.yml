# INTERNAL — triggered on push to main and manually; never on pull_request.
name: Quality Gates

on:
  push:
    branches: [ main ]
  workflow_call:
  workflow_dispatch:

jobs:
  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@v2.3.4
    with:
      java-version: '21'
      coverage-command: ./gradlew qualityCoverageManual --no-daemon --stacktrace
    secrets: inherit
